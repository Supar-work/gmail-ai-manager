{
  "name": "gmail-ai-manager",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter @gam/api --filter @gam/web run dev",
    "dev:api": "pnpm --filter @gam/api run dev",
    "dev:web": "pnpm --filter @gam/web run dev",
    "dev:desktop": "pnpm --filter @gam/desktop run dev",
    "build": "pnpm -r run build",
    "build:webapi": "pnpm --filter @gam/shared run build && pnpm --filter @gam/api run build && pnpm --filter @gam/web run build",
    "build:desktop": "pnpm --filter @gam/desktop run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "db:migrate": "pnpm --filter @gam/api run db:migrate",
    "db:generate": "pnpm --filter @gam/api run db:generate",
    "db:studio": "pnpm --filter @gam/api run db:studio",
    "db:reset": "pnpm --filter @gam/api run db:reset"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
