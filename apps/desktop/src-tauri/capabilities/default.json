{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the menu-bar shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
